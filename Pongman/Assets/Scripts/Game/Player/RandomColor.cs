using UnityEngine;
using System.Collections;

public class SetPlaeyRandomColor : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//((MeshRenderer)this.GetComponent<MeshRenderer>()).SetC
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
